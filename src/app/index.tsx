import React, { FC } from 'react';
import Card from './components/card';
import './index.scss';

const App: FC = () => {
	return (
		<div className="app">
			<Card />
		</div>
	);
};

export default App;
